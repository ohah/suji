---
title: macOS App Sandbox 자동화
description: Mac App Store 진출용 entitlements 자동 부착 — 메인 + CEF Helper 4개 helper별
---

# macOS App Sandbox 자동화

Mac App Store 진출에는 `com.apple.security.app-sandbox` entitlement가 필수. CEF는 메인
프로세스 + 4개 Helper (Browser/GPU/Renderer/Plugin) 구조라 각 helper마다 적절한
entitlements를 별도 부착해야 한다. Suji는 `config.app.sandbox: true` 한 줄로 자동 처리.

## 활성화

자동 — 별도 설정 불필요. `suji build` 실행 시 `<App>.app/` 번들의 메인 + 4개 Helper
모두 helper별 entitlements로 자동 codesign. 모든 plist에 `com.apple.security.app-sandbox`
포함.

```json
{ "app": { "name": "My App", "version": "1.0.0" } }
```

## 자동 부착되는 entitlements

| 위치 | 파일 | 핵심 key |
|---|---|---|
| 메인 앱 + 메인 binary | `assets/entitlements/main.plist` | `app-sandbox` + `cs.allow-jit` + `network.client/server` + `files.user-selected.read-write` |
| Helper (Browser) | `assets/entitlements/helper.plist` | `app-sandbox` + `cs.inherit` |
| Helper (GPU) | `assets/entitlements/helper-gpu.plist` | `app-sandbox` + `cs.allow-jit` + `cs.allow-unsigned-executable-memory` + `cs.disable-library-validation` |
| Helper (Renderer) | `assets/entitlements/helper-renderer.plist` | `app-sandbox` + `cs.allow-jit` + `cs.disable-library-validation` |
| Helper (Plugin) | `assets/entitlements/helper-plugin.plist` | `app-sandbox` + `cs.allow-jit` + `cs.allow-unsigned-executable-memory` |

CEF docs ([Wiki — Additional Configuration](https://bitbucket.org/chromiumembedded/cef/wiki/AdditionalConfiguration#markdown-header-macos))
권장 따름.

## 사용자 추가 entitlements

앱별 추가 권한 (예: 카메라/마이크/USB 접근, Apple Pay 등) 필요 시 `app.entitlements`로
사용자 plist 경로 지정. 그 파일이 모든 binary에 단독 적용 (helper별 분기 대신).

```json
{
  "app": {
    "entitlements": "my-app.entitlements"
  }
}
```

→ `codesign --entitlements my-app.entitlements` 가 모든 binary에 적용.

## 알려진 제약

- **App Sandbox 활성 시**: 사용자가 `~/Documents/myapp` 같은 임의 경로에 직접 쓸 수 없음
  (UI 통한 user-selected만). 명시 권한 필요.
- **외부 dylib 로드**: `cs.disable-library-validation`이 helper에는 있지만 메인에는 없음
  (보안 강화). 메인에서 third-party dylib 로드하려면 `app.entitlements`로 추가.
- **Network**: 메인만 `network.client/server` enabled. Helper는 inherit으로 받음.

## 검증

```bash
# 번들 빌드 후 entitlements 확인
codesign --display --entitlements - "My App.app/Contents/MacOS/My App"
codesign --display --entitlements - "My App.app/Contents/Frameworks/My App Helper (GPU).app"

# Sandbox 동작 확인 — sandbox-exec 같은 도구는 macOS 13+에서 deprecated.
# Apple 권장: Apple Configurator 또는 Xcode Console 로그.
```

## CI 자동 빌드 (후속)

GitHub Actions에서 Apple Developer ID로 sign + notarize → DMG 패키징은 별도 워크플로
([PLAN.md 우선순위 9 "앱 패키징"](../docs/PLAN.md)). 현재 `suji build`는 ad-hoc 서명
(`-`)만. 프로덕션 배포 시 `codesign --sign "Developer ID Application: ..."` 분기 필요.
