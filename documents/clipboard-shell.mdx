---
title: Clipboard & Shell
description: 시스템 클립보드 + 외부 핸들러 (Electron clipboard.* / shell.*) — 5개 진입점 모두 노출
---

# Clipboard & Shell

Electron `clipboard.*` / `shell.*` 호환 API. Frontend JS + Zig/Rust/Go/Node 백엔드 SDK
모두 노출 — 모든 호출은 `__core__` 채널로 라우팅되어 cef.zig 핸들러로 도달.

## 플랫폼 지원

| API | macOS | Linux | Windows |
|---|---|---|---|
| `clipboard.readText/writeText/clear` | ✅ NSPasteboard | ✅ GTK clipboard text | ✅ Win32 CF_UNICODETEXT |
| `clipboard.readHTML/writeHTML` | ✅ NSPasteboard `public.html` | ✅ GTK `text/html` target | stub |
| `shell.openExternal` | ✅ NSWorkspace + URL scheme 검사 | ✅ GIO default URI handler | stub |
| `shell.openPath` | ✅ NSWorkspace `file://` | ✅ GIO file URI default handler | ✅ ShellExecuteW |
| `shell.showItemInFolder` | ✅ activateFileViewerSelectingURLs: + 사전 fileExistsAtPath: | ✅ FileManager1 D-Bus `ShowItems` | stub |
| `shell.beep` | ✅ NSBeep | ✅ GDK display beep | stub |
| `shell.trashItem` | ✅ NSFileManager Trash | ✅ GIO `g_file_trash` | ✅ SHFileOperationW |

Linux는 GTK clipboard text/HTML target과 GIO default URI handler/file URI/Trash, FileManager1 D-Bus, GDK beep 경로를 사용한다.
RTF/image/buffer 같은 clipboard 확장 format은 아직 macOS 중심이며,
미지원 format은 graceful 응답 (`success: false` / 빈 값)으로 떨어진다.

## Clipboard

### Frontend JS (`@suji/api`)

```ts
import { clipboard } from '@suji/api';

await clipboard.readText();        // → string (빈 문자열이면 클립보드 빔)
await clipboard.writeText(text);   // → boolean (성공 여부)
await clipboard.clear();           // → boolean
```

### Zig 백엔드 (`suji.clipboard.*`)

```zig
const r = suji.clipboard.readText();         // ?[]const u8 — raw JSON
const r = suji.clipboard.writeText("hi");
const r = suji.clipboard.clear();
```

### Rust 백엔드 (`suji::clipboard::*`)

```rust
let r = suji::clipboard::read_text();        // Option<String>
let r = suji::clipboard::write_text("hi");
let r = suji::clipboard::clear();
```

### Go 백엔드 (`github.com/ohah/suji-go/clipboard`)

```go
import "github.com/ohah/suji-go/clipboard"
text := clipboard.ReadText()
clipboard.WriteText("hi")
clipboard.Clear()
```

### Node 백엔드 (`@suji/node`)

```ts
import { clipboard } from '@suji/node';
await clipboard.readText();
await clipboard.writeText("hi");
await clipboard.clear();
```

## Shell

### Frontend JS

```ts
import { shell } from '@suji/api';

await shell.openExternal("https://example.com");  // → boolean
await shell.showItemInFolder("/tmp/file.txt");    // → boolean (경로 존재해야 true)
await shell.beep();                                // → boolean
await shell.trashItem("/tmp/file.txt");            // → boolean
```

### 백엔드 — 4개 SDK 동일 패턴

```zig
suji.shell.openExternal("https://...");
suji.shell.showItemInFolder("/path");
suji.shell.beep();
suji.shell.trashItem("/path");
```

```rust
suji::shell::open_external("https://...");
suji::shell::show_item_in_folder("/path");
suji::shell::beep();
suji::shell::trash_item("/path");
```

```go
import "github.com/ohah/suji-go/shell"
shell.OpenExternal("https://...")
shell.ShowItemInFolder("/path")
shell.Beep()
shell.TrashItem("/path")
```

```ts
import { shell } from '@suji/node';
await shell.openExternal("https://...");
await shell.showItemInFolder("/path");
await shell.beep();
await shell.trashItem("/path");
```

## 동작 정밀

### `openExternal`

URL 검증 단계:
1. JSON unescape → `text.len + 1 > 4096`이면 false 즉시 (path 한도)
2. scheme 존재/문법 검사 — 빈 scheme 또는 control char 포함이면 false
3. macOS: `NSURL.URLWithString` + `NSWorkspace.openURL`
4. Linux: GIO `g_app_info_launch_default_for_uri`

scheme 없는 input(`"noschemejustwords"` 같은 relative URL)은 사전 차단 — `URLWithString`이
relative URL을 통과시켜 그대로 `openURL`에 보내면 macOS가 OS 알림 띄우는 문제 회피.

### `openPath`

1. 존재하지 않는 path는 false
2. macOS/Windows: 시스템 기본 앱으로 path를 open
3. Linux: `GFile` 존재 검증 → `file://` URI 변환 → GIO default application launch

등록된 기본 앱이 없거나 desktop environment가 launch를 거부하면 `false`.

### `showItemInFolder`

1. 존재하지 않는 path는 false
2. macOS: `NSURL.fileURLWithPath:` + `NSArray.arrayWithObject:` → `NSWorkspace.activateFileViewerSelectingURLs:`
   (modern API. `selectFile:inFileViewerRootedAtPath:`은 deprecated)
3. Linux: `GFile` 존재 검증 → `file://` URI 변환 → session D-Bus `org.freedesktop.FileManager1.ShowItems`

Linux에서 FileManager1 서비스가 없거나 D-Bus 호출이 실패하면 `false`.

### `beep`

- macOS: `NSBeep()` 직접 호출 — AppKit C symbol
- Linux: `gdk_display_beep(gdk_display_get_default())`

동기 호출이며, Linux에서 display가 없으면 no-op로 반환한다.

### `trashItem`

- macOS: `NSFileManager.trashItemAtURL:resultingItemURL:error:`
- Linux: GIO `g_file_trash` (freedesktop Trash spec)
- Windows: `SHFileOperationW` + `FOF_ALLOWUNDO`

존재하지 않는 path, path 한도 초과, 또는 filesystem이 Trash를 지원하지 않는 경우 `false`.

## JSON wire 안전성

text/path/url 페이로드는 모두 `escapeJsonStrFull`로 인코딩 — `\n`/`\t`/`\r` 보존,
control char `\u00XX`. 백슬래시/따옴표 round-trip 정확. UTF-8 멀티바이트(한글/이모지)
그대로 통과.

응답 디코딩(클립보드 read의 text 필드)도 동일 — 모든 control char 보존.

## 한계

- **텍스트/HTML 클립보드 플랫폼** — macOS(NSPasteboard), Linux(GTK clipboard text/html),
  Windows(CF_UNICODETEXT)는 native 동작. RTF/image/buffer 확장은 아직 macOS 중심.
- **Clipboard 텍스트 16KB 한도** — `CLIPBOARD_MAX_TEXT` 상수 + `MAX_RESPONSE`. 더 긴 텍스트는 `false` 반환.
- **URL/path 4KB 한도** — `SHELL_MAX_PATH` (NSString stack alloc 안전).
- **Linux 확장 format 잔여** — Electron의 `clipboard.readImage` / `writeBuffer` 동등 Linux backend는 향후 작업.
- **Linux shell beep 검증 경계** — GDK 호출 경로와 IPC 안정성은 E2E로 검증하지만,
  실제 청각 출력은 runner 오디오 장치 상태에 따라 자동 검증하지 않는다.

## E2E 검증

- **Clipboard**: `tests/e2e/clipboard.test.ts` — 37 케이스 (write/read/clear, 길이 한도, JSON wire, Unicode/RTL/이모지/ZWJ, 200회 stress, Promise.all 동시, 인터리브, 다중 창)
- **Linux Clipboard text/HTML**: `tests/e2e/clipboard-text-runtime.test.ts` — Xvfb에서 GTK clipboard text + `text/html` write/read/has/formats/clear round-trip
- **Linux Shell trashItem**: `tests/e2e/shell-trash-runtime.test.ts` — Xvfb/dbus에서 GIO trashItem file disappearance + missing path 검증
- **Linux Shell openExternal**: `tests/e2e/shell-open-external-runtime.test.ts` — Xvfb/dbus에서 임시 `x-scheme-handler` 등록 후 GIO launch + marker file round-trip 검증
- **Linux Shell openPath**: `tests/e2e/shell-open-path-runtime.test.ts` — Xvfb/dbus에서 임시 MIME type/default app 등록 후 GIO launch + handler가 받은 path 또는 `file://` URI marker round-trip 검증
- **Linux Shell showItemInFolder**: `tests/e2e/shell-show-item-runtime.test.ts` — Xvfb/dbus에서 fake FileManager1 서비스 등록 후 `ShowItems(file://...)` round-trip 검증
- **Linux Shell beep**: `tests/e2e/shell-beep-runtime.test.ts` — Xvfb에서 GDK beep IPC 반복 호출 안정성 검증
- **Shell**: `tests/e2e/shell.test.ts` — 32 케이스 (invalid input, valid path/URL RUN_DESTRUCTIVE, scheme 사전 차단, fixtures with spaces/한글/symlink/hidden, 100회 stress)

```bash
bash tests/e2e/run-clipboard.sh
bash tests/e2e/run-clipboard-text-runtime.sh
bash tests/e2e/run-shell-trash-runtime.sh
bash tests/e2e/run-shell-open-external-runtime.sh
bash tests/e2e/run-shell-open-path-runtime.sh
bash tests/e2e/run-shell-show-item-runtime.sh
bash tests/e2e/run-shell-beep-runtime.sh
RUN_DESTRUCTIVE=1 bash tests/e2e/run-shell.sh   # 브라우저 탭 + Finder 윈도우 + beep 발생
```
