---
title: Window Lifecycle Events (Phase 5)
description: 창 라이프사이클 이벤트 — resize/focus/blur/move/closed (Electron BrowserWindow 이벤트 대응)
---

# Window Lifecycle Events

Electron `BrowserWindow` 이벤트와 동등한 OS-level 창 이벤트. macOS는 `NSWindowDelegate`
(`windowDidResize:` / `windowDidMove:` / `windowDidBecomeKey:` / `windowDidResignKey:`)에서
직접 라우팅하며, 모든 창 생성 시 자동 부착된다. Linux/Windows는 stub (이벤트 발화 X).

## 이벤트 채널

| 채널 | 발화 시점 | Payload |
|---|---|---|
| `window:created` | 창이 만들어진 직후 | `{windowId, name?}` |
| `window:resized` | OS가 창 크기를 바꾼 후 | `{windowId, x, y, width, height}` |
| `window:moved` | 창이 이동한 후 | `{windowId, x, y}` |
| `window:focus` | 창이 키 윈도우가 됨 | `{windowId}` |
| `window:blur` | 창이 키 윈도우에서 빠짐 | `{windowId}` |
| `window:close` | close 요청 (취소 가능) | `{windowId}` (preventDefault 가능) |
| `window:closed` | 창 파괴 직후 (단방향) | `{windowId}` |
| `window:all-closed` | 마지막 창이 닫힘 | `{}` |

## Frontend JS

```ts
import { on } from '@suji/api';

on('window:resized', ({ windowId, x, y, width, height }) => {
  console.log(`win ${windowId} → ${width}x${height} at (${x},${y})`);
});

on('window:focus', ({ windowId }) => { /* ... */ });
on('window:blur',  ({ windowId }) => { /* ... */ });
on('window:moved', ({ windowId, x, y }) => { /* ... */ });
```

## Node 백엔드

```js
import { on } from '@suji/node';

on('window:resized', (data) => {
  const { windowId, width, height } = JSON.parse(data);
  // 창 크기 저장 등
});
```

## Zig 백엔드

```zig
fn onResized(event: suji.Event) void {
    // event.data: {"windowId":N,"x":..,"y":..,"width":..,"height":..}
}

pub const my_app = suji.app()
    .on("window:resized", onResized)
    .on("window:focus", onFocus)
    .on("window:blur", onBlur);
```

## Rust 백엔드

```rust
suji::on("window:resized", on_resized, std::ptr::null_mut());
```

## Go 백엔드

```go
suji.On("window:resized", func(data string) { /* ... */ })
```

## 한계

- **focus/blur는 macOS app activation에 의존**. 다른 앱이 활성 중일 때 새 창을 만들면
  `makeKeyAndOrderFront:`만 호출되고 key window 변경이 일어나지 않을 수 있다 →
  focus 이벤트 미발화. `NSApp.activateIgnoringOtherApps:YES` 호출이 필요하다면 별도 IPC.
- Linux/Windows는 모든 라이프사이클 이벤트가 stub. 향후 GTK signal / Win32 message로 확장.
- 동시 부착 가능한 창 수는 `SUJI_WINDOW_LIFECYCLE_MAX = 64`. 한도 초과 시 attach 실패하나
  창 자체는 정상 동작 (이벤트만 미발화).
